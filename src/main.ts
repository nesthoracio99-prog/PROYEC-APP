// Punto de entrada (Vite + TypeScript)
// Nota: el visor todavía vive en index.html como script monolítico.
// Siguiente etapa: migrar funciones del visor a módulos TS aquí.
export const APP_VERSION = '0.1.0';
console.log('[visor-cctv] TS entry', APP_VERSION);
